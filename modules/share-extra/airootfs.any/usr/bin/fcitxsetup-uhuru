#!/bin/bash

set_tor_bridge() {
  # Ask if the user needs to use a bridge.
  if zenity --question --no-wrap --text="Do you want to switch US keylayout and input mode? "; then

    killall fcitx
    setxkbmap us

  else
    :
  fi
}

set_tor_bridge
